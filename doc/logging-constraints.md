# 日志优化约束

> AI 协作入口：修改日志、截图通知、WebUI 日志消费、视觉/识别/匹配或设备链路前，先完整阅读本文件。
>
> 本文件是持续维护的规范，不是一次性实施记录。新增业务或上游同步引入日志时，必须补充文末的增量审计。

## 1. 权威顺序与适用范围

发生冲突时按以下顺序判断：

1. 当前已确认的用户意图和 issue 验收条件；
2. 子系统业务约束文档，例如专精业务以 `doc/mastery-constraints.md` 为准；
3. 本文件定义的日志、截图通知与消费者合同；
4. #48 冻结 ledger，作为历史范围与目标形状的不可变证据。

本文件覆盖 Python `logger.*` 调用、WebUI 日志/截图消费者、视觉与设备链路、识别与匹配链路，以及后续合入的新业务日志。日志优化只能改变可观测性，不得改变业务返回值、异常传播、重试次数、状态机、调度、通知去重或设备动作。

## 2. 冻结基线不可变

#48 的冻结 ledger 共 749 行，全部必须实施：

- 278 行 `scope_required` 是逻辑记录和实际文件字节的双 95% 体积主范围；
- 471 行 `scope_consistency` 是截止线后的语义一致性范围；
- 两种 selection basis 都是实施范围。双 95% 只决定施工优先级，不是保留旧语义的许可；
- 同一源码日志语句产生的所有消息形状采用同一合同，不得并存新旧两套语义。

`.scratch/log-baseline/` 中的原始日志、报告、声明和 ledger 已获 owner-approved waiver。不得改写报告、修改 ledger、降低冻结阈值或放松严格校验器。`doc/log-baseline.md` 只描述采集与冻结流程。

上游在冻结后新增的日志不回填 ledger。它们进入本文件末尾的“增量审计”，以同一合同判断和测试。

## 3. 日志语义合同

### 3.1 级别和所有权

- 低层例行成功静默。截图成功、模板命中/未命中、OCR 正常返回、点击/滑动成功、设备命令成功、候选遍历和单次轮询不逐条记录。
- 高层只记录有限的代表事实。一次 operation 由最了解最终结果的边界记录一次；下层不得先记录同一失败再抛给上层重复记录。
- `INFO`：真实业务状态迁移、用户可感知动作完成，或一次高层 operation 的最终成功。
- `WARNING`：一次 operation 经内部重试后恢复，或发生可继续但需要关注的降级。内部每次失败静默，恢复后由 owning boundary 最多记录一次。
- `ERROR`：重试耗尽或 operation 最终失败，由最终处理边界记录一次。意外异常保留一条完整因果链，不在多层重复 traceback。
- `DEBUG` 也必须有语义价值和有限字段；它不是对象 dump、循环刷屏或临时 print 的避难所。

同一状态的重复观察静默；只有真实状态变化才记录。重试、轮询和进度属于一个 operation，不因循环次数变成多个业务事件。

### 3.2 有界字段

日志优先使用稳定的 `key=value` 事实。字段必须可枚举或显式有界，并满足：

- 不记录任意对象、DB 行、完整计划字典、OCR 原始对象、无限列表或候选全集；
- 不记录 token、账号、UID、cookie、请求头、设备凭据或其它秘密；
- 字符串使用业务上天然有界的标识或名称；列表只记录经合同挑选的少量代表项；
- 不得用 `repr`、`str(object)` 或简单截断来伪装无界 dump；
- 需要关联时使用稳定 ID，例如 `plan_id`、`task_type`、`scene`，不展开整个实体。

状态迁移建议形状：

```text
operation={operation} entity_id={entity_id} from_state={from_state} to_state={to_state} trigger={trigger} action={action} result={result}
```

字段按具体业务删减，不为填模板制造无意义字段。

### 3.3 代表事实

一次识别或匹配 operation 需要诊断上下文时，只保留去重后的有限代表项：

- 距离最近的 3 项；
- 最慢的 1 项；
- 首个无效或缺失项；
- 最终选中的 1 项。

各角色可能指向同一项，去重后最多 6 项。不得因为候选很多而逐候选记录，也不得靠截断候选 ID 列表保留旧噪音。

### 3.4 长任务进度

短任务不发进度日志。确需报告的长 operation：开始后满 5 分钟才允许首条进度，此后最多每 15 分钟一条；完成或失败由 owning boundary 记录最终结果。不得新增固定高频心跳。

### 3.5 中文正文与稳定字段

项目代码自行编写、面向用户或维护者阅读的自然语言正文使用中文。保留的权威记录不能只有英文 `key=value`，应采用“对象、动作与结果：关键数据；恢复结果或下一步”的语义骨架，例如：

```text
场景识别重试后恢复：operation=scene_recovery outcome=recovered attempt=2
```

- 中文正文表达对象、动作和结果；稳定的英文 `key=value` 字段、有限枚举和事件标识继续承担检索与消费者兼容职责。
- 日志等级、时间、源码位置、函数名、协议/API 名、异常类型、类名/函数名、路径、设备标识、第三方工具名，以及 Python traceback、ADB、MAA 等第三方原始错误文本允许保留英文。
- 无法安全翻译且必须原样检索的外部返回值可以保留；不能仅凭出现 ASCII 字母判定违规。
- 只有原因已确定且用户确实可以行动时才写“下一步”；原因不确定时只描述已知失败，不猜测修复建议。
- 消费者不得解析中文句子，也不得保留旧英文自然语言的双读 fallback；跨端或数据库关联只依赖稳定字段、有限枚举或事件标识。

## 4. 视觉、识别、匹配、截图与设备链路

- 低层截图、裁剪、颜色计算、模板查找、OCR、匹配候选和设备命令的正常成功路径静默。
- 高层记录最终识别结果、真实 Scene 迁移、最终选择、恢复后的降级或重试耗尽；不得逐层重复同一事实。
- 保留原业务返回能力：日志删除或重构不得吞掉返回值、异常、截图对象、候选结果或设备响应。
- 诊断信息使用 §3.3 的代表事实，不能恢复对象 dump。
- 截图文件写入完成是通知消费者的前置条件；不能让前端读取半写文件。

## 5. Scene 快照与 WebUI 合同

WebUI 的 Scene 快照是事件驱动合同：

1. 页面打开时读取一次最新截图；
2. WebSocket 每次重新连接成功时读取一次最新截图；
3. 预览从关闭变为启用时读取一次最新截图；
4. 运行中只有真实 Scene 变化且对应截图已经写完后，才通知前端刷新一次；
5. 同一 Scene 的后续截图不重复刷新。

禁止新增定时轮询、五秒兜底、逐截图推送或通用截图事件。日志接口的“最近 100 行”必须按实际逻辑行裁剪，并保持移动端连续阅读合同。

服务端事件名、载荷和前端消费者必须成对修改。新增或改名任何事件前，先搜索所有生产者和消费者，并以合同测试锁定双方。

文本日志与反馈沿用现有公开合同：

- `from arknights_mower.utils.log import logger` 继续可用；文件是 UTF-8 普通文本，保留时间、源码位置、英文等级、函数和消息外壳，不冻结旧消息正文；
- `runtime.log` 与小时轮转的基本命名不变。`get_log_by_time(target_time, time_range=1)` 以文件内真实首末记录时间判断范围，返回存在的 `Path`，按记录时间稳定排序；调用方不得重新按文件名排序或解析文件名推断时间；
- 反馈邮件直接附加查询返回的原始文本文件，不打包、不转成 SQLite/manifest/摘要；反馈正文只进入邮件正文，不得写入 logger 或终端输出；
- 服务端与前端均按实际换行裁剪最近 100 行，CRLF 和消息尾随换行不得制造额外空行；桌面端保留完整文本，移动端只移除既有时间前缀并保留续行；
- 正常解释器退出应幂等停止现有 `QueueListener`，让已经入队的记录完成既有投影。这不新增强制 flush 接口，不承诺并发生产者退出后的记录、活动文件不可变快照或精确查询起止时间。

## 6. 专精子系统映射

修改 `mastery.py`、`mastery_reader.py`、`mastery_db.py`、专精相关排班路径或其日志前，必须完整阅读 `doc/mastery-constraints.md`。其中截图权威、状态机、恢复矩阵、通知去重、排班 gate 和 `enable_mastery` 语义均高于日志整形。

- 房间/OCR 的重复读取、矩阵重试、倒计时刷新同值和不变状态静默；不得因“保守等待”制造每轮日志。
- 真实计划状态迁移可记一条 `INFO`，使用 `plan_id`、`from_state`、`to_state`、`trigger`、`action`、`result` 等有限事实，不展开计划或房间对象。
- 内部每次 OCR/换人/读取重试静默；重试后恢复由拥有该 operation 的边界最多记一条 `WARNING`，耗尽由最终处理边界最多记一条 `ERROR`。
- 通知日志只在通知确实发送时记录，并服从 `mastery_notify` 去重；不得把被去重的尝试写成已发送。
- “截图为准”意味着日志必须描述实际读取与最终决策，不能把 DB 缓存提示写成已确认的游戏状态。
- 专精邮件正文与对应 `INFO` 日志需要共享文案时，保持 `doc/mastery-constraints.md` 规定的发送时机、档位、技能真名和完成时间口径。

## 7. Rebase、合并与冲突处理

- `fork/alpha` 已存在的产品功能和业务行为是新基准；日志分支保留基线采集、严格 ledger 校验、文档、测试和本文件合同。
- 不得整块选择 ours/theirs。逐个调用点确认业务所有者、最终 handling boundary、消费者和测试。
- 文本自动合并不代表语义兼容；上游新增的 `logger.*`、截图事件或消费者都要进入增量审计。
- 若业务文档与旧日志形状冲突，保留业务不变量，再调整日志形状和测试。
- 不改写或强推 `alpha`。日志分支需要重写时，只更新 fork 的 `feat/log-optimize`，并使用带明确期望值的 `--force-with-lease`。

## 8. 测试与验收

日志改造使用测试先锁定合同，至少覆盖受影响的以下行为：

- 低层成功静默，高层最终结果一次；
- 不变状态静默，真实迁移一次；
- 重试中间过程静默，恢复一次 warning，耗尽一次 error；
- 有界字段和代表事实去重上限；
- 页面打开、WebSocket 重连、预览重新启用各读一次最新截图；
- Scene 变化一次、同 Scene 静默、文件写完才通知/读取；
- 最近 100 行和移动端合同；
- 生产者与消费者事件一致。

改动后按范围运行专项测试、全量 Python 测试、Ruff lint/format、前端测试和构建。测试断言日志语义与次数，不把时间、路径或随机数据写成脆弱快照。

## 9. 计数与报告口径

每个实施切片报告改造前后的逻辑记录数和实际文件字节，并明确样本类型：

- #48 原始两小时窗口是历史基线；
- 固定 fixture/replay 可以做严格配对的确定性对比；
- 新代码实机窗口只有在环境、负载、时长和配对条件一致时才可称严格对照。

上游业务改变了代表性任务流程、日志语义或日志消费者后，旧 before 窗口不再是严格配对样本。此时仍可用于历史规模与热点定位，但必须明确披露，不得伪造严格两小时 before/after。

## 10. 上游增量审计

每次同步上游后新增一节，至少记录基线提交、涉及子系统、日志分组、决策、测试和状态。冻结 ledger 不随之变化。

### 2026-08-17：`fork/alpha` `ec64f26f`

- 状态：已完成逐调用点审计并按本合同调整。
- 业务范围：全自动专精长期开发、训练室读取/恢复/排班、服务端与 WebUI 日志页。
- 可比性预警：该基线改变了代表性任务流程并新增大量生产日志；#48 的旧 before 窗口只能作为历史基线，不能冒充与 `ec64f26f` 严格配对的样本。
- 范围计数：`alpha` 相对旧基线新增 110 个 `logger.*` 调用点。审计后其中 60 个例行/重复调用点静默，50 个高层或最终 handling boundary 调用点保留并整形；另将 3 个既有专精 DB 错误处理器统一为同一异常合同。此计数是源码调用点差异，不冒充运行期逻辑记录或文件字节。
- 重试合同：OCR 五次重读和换人五次原地重试的业务次数不变；中间尝试静默，恢复汇总一条 `WARNING`，耗尽汇总一条最终记录。OCR 保守降级是 `WARNING`，换人重试耗尽是 `ERROR`。
- 状态合同：训练中同状态、B8 不采纳等待、已在队首、锁定位和换人公式等无变化观察静默；真实计划迁移、最终训练开始、收取迁移和实际换人保留有限 `key=value` 事实。
- 隐私/对象合同：删除 `/task` 的完整请求与 `SchedulerTask` dump、交易逐次落库成功日志和数据库删除结果字典；受控的任务拒绝静默返回，意外异常在 HTTP 或 DB 最终边界保留一条因果链。
- WebUI 冲突：`Log.vue` 同时保留 `alpha` 的数据库管理初始化和 #49 的一次性最新 Scene 快照读取；打开、重连、重新启用预览、Scene 变化、同 Scene 静默及写完再读合同不变。
- 测试：新增 10 个增量合同测试，覆盖 OCR 首次成功静默、恢复/耗尽、同状态静默、换人恢复/耗尽、交易成功/重复静默、任务请求不 dump 和数据库删除有限摘要；按 TDD 先观察失败，调整后相关三组测试 30/30、专精/视觉专项 413/413、全量 Python unittest 493/493、前端 Vitest 9/9 通过，Ruff lint/format、Prettier 和 Vite build 通过。
- 剩余限制：本次没有与 `ec64f26f` 严格配对的两小时实机 replay；#49 已有的确定性 Scene fixture 和历史 before 窗口继续按 §9 的原口径报告，不据此声称新 `alpha` 的实机降幅。

### 2026-08-17：#50 调度、重试与 SQLite 消费者

- 基线：实施起点为 `e09c0021`，其共同上游仍是 `fork/alpha` `ec64f26f`；同步前后 feature 相对 `fork/alpha` 均为 0 behind，本节没有改写 `alpha`。
- 冻结范围：同时实施 `object_dump`、`scheduler_state_sqlite`、`retry_error_duplication` 三类共 242 行，其中 123 行 `scope_required`、119 行 `scope_consistency`；77 行改为静默，165 行改为有限业务事实。冻结 ledger、声明、原始报告和原始日志未修改。
- 生产者合同：调度任务只保留 `task_type/scheduled_at/room/room_count/adjusted`，dorm、心情计划、房间选人、MAA 终态和任务状态只保留各自有限字段；MAA 进度、候选遍历、SQLite 例行写入成功、维护缓存命中/跳过和内部轮询静默。
- 重试合同：内部尝试静默；场景、心情读取和排班恢复只记一条 `WARNING`，耗尽由最终处理边界记一条 `ERROR`。异常类型、重试次数、设备动作和传播路径保持不变；删除 `save_exception + logger.exception` 双写和完整 traceback 的 SQLite 副本。
- SQLite 合同：`emit_log_event` 是文本与 `log` 表的单一生产者，仅白名单允许 `task_dispatch/started` 与 `missed_order/detected`；终态重试使用同一投影边界。`task` 列改为只含 `task_type/scheduled_at/room/adjusted` 的有限 JSON，不再写 `SchedulerTask.__repr__`。例行事件会被拒绝，不能把文本噪声转移到 SQLite。
- 消费者合同：漏单分析只读有限 JSON，不保留旧 `SchedulerTask` 双读 fallback；运行期文本通过 `get_log_by_time` 公共时间查询取得并识别 `INFO/WARNING/ERROR`。通用数据库工具强制只读 `SELECT`、`PRAGMA query_only`、HTML 转义和最多 100 行。
- 历史 before 证据：三次原始两小时窗口的全量文本分别为 69,548 / 10,572,140 bytes、540,302 / 74,842,448 bytes、55,376 / 8,787,188 bytes；SQLite 分别为 44 / 26,022 logical text bytes、19 / 20,074、29 / 32,127。#50 三类调用点在文本中的历史贡献分别为 3,163 / 1,099,777 bytes、2,737 / 750,905 bytes、6,823 / 1,808,910 bytes。前两窗沿用 #48 waiver，第三窗通过严格校验；这些数字只作历史规模和热点证据。
- 确定性合同 fixture：固定覆盖一次任务分派、一次 dorm 任务生成、MAA 进度与终态、重试恢复与耗尽、一次漏单和一次 SQLite 例行成功；按固定运行期前缀、UTF-8、CRLF 物化。文本由 18 条 / 1,966 actual file bytes 降为 6 条 / 847 bytes；SQLite `log` 由 9 行 / 811 logical text bytes 降为 4 行 / 615 bytes。该 fixture 只比较日志合同，不模拟业务时长或负载。
- 测试：#50 专项覆盖单一文本/SQLite 投影、白名单、有限任务 JSON、漏单新消费者、公共时间查询、数据库只读 100 行上限、dorm/Operator 摘要、MAA 终态、场景与排班重试恢复/耗尽，以及例行 SQLite 成功静默；每个新合同先观察失败再实现。最终专项 80/80、全量 Python unittest 519/519、前端 Vitest 9/9 通过，Ruff lint/format、Prettier 和 Vite build 通过。
- 剩余限制：没有采集与当前 feature 严格配对的两小时 after 实机窗口，因此不得把历史 before 与上述 fixture 拼成两小时降幅，也不声称 CPU/RSS 或实机吞吐改善。

### 2026-08-17：#51 文本日志、时间检索与反馈兼容链路

- 基线：#51 从 #50 修订提交 `ea27ce21` 开始；共同上游仍是 `fork/alpha` `ec64f26f`，实施前 feature 相对它为 0 behind。#50 修订只让漏单分析保留 `get_log_by_time` 的时间顺序，不混入本切片。
- 文本与轮转：公共 `logger` 导入、UTF-8 普通文本外壳、`runtime.log` 和 `runtime.log.YYYY-MM-DD_HH` 小时轮转名称由隔离进程合同锁定。没有更换 logger、文件格式、等级或消息正文。
- 时间检索：`get_log_by_time(target_time, time_range=1) -> list[Path]` 从每个 `runtime.log*` 的真实首末日志记录判断与目标前后范围是否重叠；首部顺序读取、尾部反向分块读取，不按文件名推断时间，也不全量扫描文件正文。跨小时、任意兼容段名和活动 `runtime.log` 按 `(首记录时间, 末记录时间, 文件名)` 稳定排序，返回项均为存在且可直接附加的原始文件。
- 反馈：HTTP 边界与 `submit_issue` 不再记录或打印完整请求/description；正文仍进入正常邮件 HTML，Bug 反馈直接附加时间查询返回的原始文本文件，附件字节和文件名不变。没有 ZIP、SQLite 导出、manifest、摘要或其它诊断载体。
- WebSocket 与展示：服务端和前端用真实换行语义处理消息，尾随 LF/CRLF 不再占用一行；两端继续只保留最新 100 行。桌面完整文本、移动端去时间前缀并保留 traceback/续行的既有展示合同不变。
- 正常停止与退出：`/stop` 仍按既有事件、线程等待、状态保存和线程引用清理流程正常停止 Mower，停止后公共 logger 继续可用；现有 `QueueListener` 注册幂等 atexit 收口，使已入队记录在正常解释器退出时完成现有 handler 投影。未建设统一日志运行时、队列平台、强制 flush、不可变活动文件快照或历史查询产品。
- 冻结范围：只读核验原 ledger 仍为 749 行且全部 include，其中 278 行 `scope_required`、471 行 `scope_consistency`。两种 selection basis 统一经过同一文本格式、时间查询、附件与 WebSocket 消费链路，不存在旧格式旁路；原始报告和 ledger 未修改。
- 确定性计数：固定反馈输入下，旧 HTTP + tool 两条完整正文 DEBUG 投影为 2 条逻辑记录 / 474 actual file bytes（UTF-8 + CRLF），现为 0 条 / 0 bytes。该计数只覆盖本切片删除的正文泄漏；公共 logger 的代表性 INFO/WARNING 文本继续原样落盘，不用删除正常记录换取数字。
- 测试与审查：#51 专项 Python 11/11、全量 Python unittest 531/531、相关前端 Vitest 10/10 通过，Ruff lint/format、Prettier 和 Vite build 通过。Standards 审查的完整外壳边界识别与测试重复、Spec 审查的正常停止与跨端 WebSocket 展示链路共 4 项发现均已修正；跨端测试由 Python 公共 logger 的真实发送链路与 Vitest 共同校验同一 payload fixture。
- 可比性与限制：#48 三个历史窗口的全量文本仍为 69,548 / 10,572,140 bytes、540,302 / 74,842,448 bytes、55,376 / 8,787,188 bytes，只作历史规模证据。没有采集与当前 feature 严格配对的两小时 after，也不把固定反馈 fixture 冒充两小时降幅；真实三组两小时终验留给 #52。

### 2026-08-17：#121 中文正文与统一语气补差

- 固定基线：实施前审查起点为 `38560876465a273fc5457c468452e7f416c337dc`；同步时 `fork/alpha` 为 `ec64f26ff7c2fc2d8f5de9acbd55e8849607c856`，feature 相对它为 0 behind。未修改、提交或推送 `alpha`。
- 审计范围：只读核验冻结 ledger 仍为 749 行且全部 include，其中 278 行 `scope_required`、471 行 `scope_consistency`。审计当前代码中由两类 ledger 行演化而来的保留日志、冻结后新增的专精日志，以及 #49/#50/#51 保留生产者；已经静默或删除的旧日志没有恢复。原始日志、报告、声明和 ledger 未修改。
- 判断口径：确定性 AST fixture 固定 142 个直接项目生产者和 4 个动态权威生产者，共 146 个待补中文正文的生产者条目；按“项目自有自然语言”判断，不以包含 ASCII 字母判断。实施后固定范围内项目自有英文自然语言正文为 0 个。
- 允许保留英文：英文日志等级和既有文本外壳；稳定字段名；有限枚举/事件标识；协议/API、异常类型、类名和函数名；路径、设备标识和第三方工具名；traceback、ADB/MAA/第三方原始错误；必须原样检索且无法安全翻译的外部返回值。以上分类不计入项目自有英文正文。
- 消息形状：保留记录采用“中文对象、动作与结果：稳定英文事实；已确定的恢复结果或下一步”。重试恢复与耗尽、真实状态变化、高层成功、最终异常、确定材料不足和原因不确定失败分别有生产者级合同；只有 `reason=insufficient_material` 写明“下一步：请补足专精材料后重试”，不确定失败不编造建议。
- 消费者迁移：SQLite 白名单和 `task` 有限 JSON 形状不变；漏单查询从依赖消息开头迁移为查找稳定的 `event=missed_order state=detected` 事实，不解析中文且不保留旧英文正文 fallback。共享 WebSocket fixture 改为中文正文，服务端仍保留最近 100 行；桌面端显示完整文本，移动端只移除时间前缀。UTF-8 文件、`get_log_by_time`、原始反馈附件、正常停止与退出合同不变。
- 前序切片归属：视觉/设备与 Scene 生产者属于 #49 补差；调度、重试、SQLite 及漏单消费者属于 #50 补差；文本/WebSocket/反馈链路属于 #51 补差；专精生产者属于 `fork/alpha` 增量审计。本票只补中文语气和消费者兼容，不重写前序提交或恢复已删除日志。
- 确定性计数：固定 7 条代表记录按同一文本外壳、UTF-8 编码和 CRLF 换行物化，汉化前后逻辑记录为 7 / 7，实际文件字节为 988 / 1,228。中文 UTF-8 使字节增加 240，本票不设降幅指标；该 fixture 不是实机样本，更不是两小时样本。
- TDD 与验证：审计测试第一次执行时任务派发生产者已完成首个红绿循环，仍报告 145 个确定性违规；随后真实业务状态变化、高层最终成功、重试恢复、重试耗尽、唯一 traceback、原因确定且可行动的失败、原因不确定且不得生成下一步的失败均先观察红灯再修改实现。最终 #121 与消费者专项 206/206、全量 Python unittest 536/536、前端 Vitest 10/10 通过，Ruff lint/format、Prettier 和 Vite build 通过。工作树已有未提交的前端依赖刷新使新版 `vite-plugin-inspect` 与 Vitest 内嵌 Vite 在启动时不兼容；Vitest 验证期间临时排除 Inspect 后通过，配置随即恢复，依赖清单与 lockfile 均未纳入本票。
- 已知限制：静态计数衡量固定权威生产者条目，不代表运行期频次；#48 历史窗口继续只作历史规模证据。未采集与当前 feature 严格配对的真实运行窗口，也未执行或伪造 #52 的三组两小时终验；#52 仍待单独执行。
